package com.AccountReceivableManagement.cdc.mapping;

public enum FieldType {
    STRING,
    INTEGER,
    LONG,
    DOUBLE,
    BIG_DECIMAL,
    BOOLEAN,
    UUID,
    LOCAL_DATE,
    LOCAL_DATE_TIME,
    ENUM,
    JSON
}
